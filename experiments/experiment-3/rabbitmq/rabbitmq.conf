# Use internal backend for authentication (username/password check).
# Use topic auth backend for all authorization — enforces routing key
# patterns on topic exchange operations; allows everything else.
auth_backends.1.authn = internal
auth_backends.1.authz = rabbit_auth_backend_topic
