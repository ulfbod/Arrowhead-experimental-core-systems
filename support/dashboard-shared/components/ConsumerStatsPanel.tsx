// Stub for standalone testing. Experiment dashboards symlink over this with
// their own implementation via the canonical file list in CLAUDE_EXPERIMENTS.md.
export function ConsumerStatsPanel(): null { return null }
